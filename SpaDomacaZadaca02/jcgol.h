#pragma once
#include <SFML/Graphics.hpp>

class jcgol
{
private:

public:
	//these functions are extremely fast
	void display(sf::RenderWindow& window);
	void display_random(sf::RenderWindow& window);
};

